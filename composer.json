{
  "require-dev": {
    "phpunit/phpunit": "^11.0",
    "squizlabs/php_codesniffer": "^3.9"
  },
  "autoload": {
    "psr-4": {
      "QuickPOS\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "QuickPOS\\Tests\\": "tests/"
    }
  },
  "scripts": {
    "test": "phpunit tests",
    "phpcs": "phpcs",
    "syntax-check": "php -r \"$it = new RecursiveIteratorIterator(new RecursiveDirectoryIterator('.')); foreach ($it as $f) { if ($f->getExtension() === 'php' && strpos($f->getPathname(), DIRECTORY_SEPARATOR.'vendor'.DIRECTORY_SEPARATOR) === false) { passthru('php -l ' . escapeshellarg($f->getPathname()), $c); if ($c) exit($c); } }\""
  }
}
